# Example environment variables for local development
# Copy this file to .env and fill in your actual values.

# Django Application Settings
SECRET_KEY=your_django_secret_key_here

# Database Connection Settings (for Django's settings.py)
DB_NAME=mydatabase
DB_USER=mydbusername
DB_PASSWORD=mydbpassword
DB_HOST=mydbhost
DB_PORT=5432

# PostgreSQL Database Service Initialization Settings (for docker-compose.yml db service)
# These define the initial database configuration for the Postgres container.
POSTGRES_DB=mydatabase
POSTGRES_USER=mydbusername
POSTGRES_PASSWORD=mydbpassword